import { createContext } from "react";
import React from "react";
export const EventContext = React.createContext<any>(undefined!);
export const EventDispatchContext = React.createContext<any>(undefined!);
export const SettingsContext = React.createContext<any>(undefined!);
export const SettingsDispatchContext = React.createContext<any>(undefined!);